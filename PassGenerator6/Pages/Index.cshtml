@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}


<h1><b>PASS GENERATOR</b></h1>
<h2><b>Kaiky Silva Pariz</b></h2>
<h4><b>Dicas para senha:</b></h4>
<ul>
    <li>Dica 1:Tente senhas maiores</li>
    <li>Dica 2:Use caracteres e números</li>
    <li>Dica 3:Não use senhas comuns</li>
</ul>
<ul>
    <li>Exemplo 1: #Tr0ng_Plss!</li>
    <li>Exemplo 2: Secur1tyR0ck$</li>
    <li>Exemplo 3: P@55w0rdPr0t3ct!</li>
</ul>
<p>Agora você vai ver o nosso Software gerador de senha:</p>
<a href="https://password.kaspersky.com/pt/">Teste a segurança da sua senha</a>

<div class="text-center">
    <button id="generatePasswordBtn" class="button-12" onclick="generatePass()">Generate Random Password</button>
    <input type="number" id="passLength" min="1" value="12" />
    <p id="passwordDisplay"></p>
    <img src="https://cdn-icons-png.flaticon.com/512/696/696975.png" />
</div>

<style>

    a{
        text-decoration: none;
        color: #000;
    }
    a:hover{
        font-weight: 700;
    }

    #img{
        max-width: 360px;
    }

    .text-center{
        line-break: revert-layer;
        word-break: break-all;
    }
    body{
        background-color: bisque;
    }
    h1{
        font-size: 100px;
        font-weight: 500;
        color: black;
    }
    /* CSS */
    .button-12 {
        display: flex;
        flex-direction: column;
        align-items: center;
        padding: 6px 14px;
        font-family: -apple-system, BlinkMacSystemFont, 'Roboto', sans-serif;
        border-radius: 6px;
        border: none;
        background: #6E6D70;
        box-shadow: 0px 0.5px 1px rgba(0, 0, 0, 0.1), inset 0px 0.5px 0.5px rgba(255, 255, 255, 0.5), 0px 0px 0px 0.5px rgba(0, 0, 0, 0.12);
        color: #DFDEDF;
        user-select: none;
        -webkit-user-select: none;
        touch-action: manipulation;
    }

    .button-12:focus {
        box-shadow: inset 0px 0.8px 0px -0.25px rgba(255, 255, 255, 0.2), 0px 0.5px 1px rgba(0, 0, 0, 0.1), 0px 0px 0px 3.5px rgba(58, 108, 217, 0.5);
        outline: 0;
    }
</style>



@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        function generatePass() {
            var passLength = $("#passLength").val();
            $.ajax({
                url: '@Url.Page(null, "GenerateRandomPassword")',
                type: "GET",
                data: { passLength: passLength },
                success: function (password) {
                    $("#passwordDisplay").text("Random Password: " + password);
                }
            });
        };
    </script>
}
